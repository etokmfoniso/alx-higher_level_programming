#!/usr/bin/python3
def delete_at(my_list=[], idx=0):
    new_list = []
    for item in my_list:
        my_list[idx] = item
        if idx < 0 or idx > len(my_list) - 1
            return my_list
        else:
            if
